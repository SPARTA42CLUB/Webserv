services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    image: nginx:test
    container_name: nginx
    ports:
      - "9090:9090"
      - "9091:9091"
    volumes:
      - ./conf:/etc/nginx/conf.d
      - ./logs:/var/log/nginx
